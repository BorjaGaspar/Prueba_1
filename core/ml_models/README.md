# ML Models Directory

This directory contains the trained Machine Learning model files:

- `modelo_niveles.pkl` — Trained Random Forest classifier
- `columnas_modelo.pkl` — Column order expected by the model

These files are generated by `IA_MachineLearnig/entrenar_modelo.py`.
